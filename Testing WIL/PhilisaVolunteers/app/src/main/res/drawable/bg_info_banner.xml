<?xml version="1.0" encoding="utf-8"?>
<!-- New drawable, not in the original 15 — needed for the Volunteer/Admin login notice banners
     (Figs 55, 67). Background colour is applied at runtime (teal for Volunteer, amber for Admin). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/status_info_bg" />
    <corners android:radius="12dp" />
</shape>
