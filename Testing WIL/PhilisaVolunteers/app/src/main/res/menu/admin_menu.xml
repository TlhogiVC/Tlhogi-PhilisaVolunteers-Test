<?xml version="1.0" encoding="utf-8"?>
<!-- Item IDs MUST match the destination IDs in admin_nav_graph.xml exactly —
     BottomNavigationView.setupWithNavController() matches menu item IDs to nav graph
     destination IDs directly, it does not use a separate mapping. -->
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <item
        android:id="@+id/adminDashboardFragment"
        android:icon="@drawable/ic_admin_overview"
        android:title="@string/nav_overview" />
    <item
        android:id="@+id/manageVolunteersFragment"
        android:icon="@drawable/ic_admin_volunteers"
        android:title="@string/nav_volunteers" />
    <item
        android:id="@+id/manageActivitiesFragment"
        android:icon="@drawable/ic_activities"
        android:title="@string/nav_activities" />
    <item
        android:id="@+id/manageAnnouncementsFragment"
        android:icon="@drawable/ic_admin_posts"
        android:title="@string/nav_posts" />
    <item
        android:id="@+id/manageActivityApplicationsFragment"
        android:icon="@drawable/ic_admin_applied"
        android:title="@string/nav_applied" />
</menu>
