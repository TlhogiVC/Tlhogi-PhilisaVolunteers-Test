<?xml version="1.0" encoding="utf-8"?>
<!-- Item IDs MUST match the destination IDs in volunteer_nav_graph.xml exactly —
     BottomNavigationView.setupWithNavController() matches menu item IDs to nav graph
     destination IDs directly, it does not use a separate mapping. -->
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <item
        android:id="@+id/homeFragment"
        android:icon="@drawable/ic_home"
        android:title="@string/nav_home" />
    <item
        android:id="@+id/activitiesFragment"
        android:icon="@drawable/ic_activities"
        android:title="@string/nav_activities" />
    <item
        android:id="@+id/scheduleFragment"
        android:icon="@drawable/ic_schedule"
        android:title="@string/nav_schedule" />
    <item
        android:id="@+id/communityFragment"
        android:icon="@drawable/ic_community"
        android:title="@string/nav_community" />
    <item
        android:id="@+id/profileFragment"
        android:icon="@drawable/ic_profile"
        android:title="@string/nav_profile" />
</menu>
